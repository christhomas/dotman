version: 2

project_name: dotman

before:
  hooks:
    - go mod tidy

builds:
  - main: .
    binary: dotman
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - formats:
      - tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

homebrew_casks:
  - name: dotman
    binaries:
      - dotman
    repository:
      owner: antimatter-studios
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: https://github.com/christhomas/dotman
    description: "Git-backed dotfile workflow manager"
    hooks:
      post:
        install: xattr -dr com.apple.quarantine "#{staged_path}/dotman"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"
